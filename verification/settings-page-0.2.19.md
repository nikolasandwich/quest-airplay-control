# 0.2.19 设置第二页

用户要求底部语言改为设置，进入第二页，收纳相对射线、辅助对齐等低频选项并增加说明；同时明确要求辅助对齐默认开。

底部现在为两排九个按钮：第一排连接鼠标、启用/暂停、上一条、下一条、恢复控制；第二排左拖、点击、右拖、设置。设置第二页有返回投屏、语言及当前选择、相对射线开关、辅助对齐开关、辅助速度、全部主控按钮的功能说明。原帮助入口也可进入设置。设置内容可滚动，系统返回键可返回主页面。

使用同一Activity中的独立全屏页面，背景不透明并拦截点击。保留底层视频Surface，避免仅为进入设置而拆建解码输出；底层主界面的无障碍节点隐藏。进入设置会撤销HID焦点和控制授权，停止辅助及采样，隐藏射线输入层，设置页滚动不经过HID。返回投屏后不会自动重新启用鼠标控制。

相对射线和辅助速度沿用偏好。新增alignment_enabled偏好默认true，符合本次用户变更；显式关闭会保存。设置页开关展示保存的意愿，实际辅助暂时停用；返回前台投屏才恢复辅助处理，且仍需已启用控制、有效射线目标、可靠识别、视频新鲜和传输就绪。暂停/PiP继续停辅助。“恢复控制”明确保存辅助关闭。未改HID相对输入合并或解除原有输入门禁。

设置页打开状态保存在Activity状态中，应用语言变更导致页面重建后仍留在设置。原长按射线诊断入口随射线按钮移入设置；用户长按后返回主页面并尝试已有只读采样，不自动移动鼠标。

74项本机测试全部通过，包含设置页面打开/暂停辅助/返回仍为同一Surface、系统返回键、辅助关闭保存、保存状态重建设置页，以及从底部设置进入语言选择的完整界面路径。新增145键的四套资源与生成回退检查通过；原网页五地区失联回归通过。APK及Android测试APK构建成功，lint通过（有既有警告）。尚未安装或推送，无真机页面实看或自动输入。安装前同步短暂中断窗口，保留0.2.18回退包。

APK SHA256：b7748fc33f79be9a8c65fbbec67dd2c7a5e09879004a4f09696a83e2e905ebd3。
