# 0.2.10重捕获候选：已构建，未部署

用户实测0.2.9“比上一版快，但未完全重合就停”，随后明确界面出现“识别未确认”时鼠标不继续纠偏。已接受“可靠时更快，不稳立刻停止”的方向。

## 已查明与不能从旧日志推断的部分

0.2.9的PointerIdentityTracker在一个无候选/低分观察后即reset；此后必须重新满足至少三帧、射线和箭头均移动4采样像素且同向。目标已停稳时，这个初始身份门槛可能长期无法满足。RayAlignment同时每次不可信都reset纠偏预算。

真机PID7847日志11:42:56至11:43:08持续身份未确认，PixelCopy成功，VideoDecoder计数继续增长、queue=0。这些观察已经通过前台/armed/近期视频总门禁，同段未看到新host suspend或断连。用户将提示与停住直接关联，结合代码可确认身份门禁是该情形的停止点。

旧日志只每秒采样身份布尔值，不记录每帧失败原因、坐标与停止状态，无法判断最初由短暂无候选、低分、跳变还是退出重置触发，也不能排除未采样时刻的其他门禁。已记录的一段纠偏末次距离13.7采样像素，随后失锁；这不是用户每次停止时的精确剩余距离。不能据此断言所有停顿都是同一原因。

另有可解释的小误差：控制目标允许3采样像素抖动容差，完成容差也为3像素，检测位置取箭头轮廓左上角而非经过标定的实际热点。它们可造成少量偏差，但不能拿来解释几十像素的停顿。当前仍未证明精确重合。

## 候选修改

- 初始身份门槛保留。已经可靠锁定后，仅在最后可信观察500ms内，可用历史位置辅助重捕获：两次唯一候选、形状分数至少.90、距历史位置最多12采样像素、两次位置相距最多3像素，才重新输出可信状态。空窗与第一帧均不发纠偏。
- 多候选歧义、过期、倒序/重复观察、过大跳变不走历史恢复捷径；长时间失锁后仍需要可靠新参考，不能把历史预测当作真实位置。
- 身份不可信时暂停纠偏，保留同一目标的步数、超时和无进展预算；重捕获不会无限重置24步/10秒保护。明确终止状态不自动无限重试。
- UI区分暂未看到、轮廓不清、多候选、正在重捕获、等待移动相关性、视频帧等待、蓝牙就绪、反馈等待、容差完成和预算停止。
- 日志追加目标编号、状态转换、可信/未可信候选的形状分数与距控制目标距离、控制目标和实时射线坐标、传输就绪及耗时。未可信候选距离只作诊断，不参与控制，也不是实际指针位置保证。

29项JVM测试、APK/Android测试APK构建、lint通过。新增短缺帧两帧恢复、歧义清除历史、过期/远处/弱候选拒绝、重复帧不能完成恢复、观察中断不能重置目标预算。真实旧采样回放四组唯一候选0/30/21/20、可信0/18/11/8，与旧版一致；说明这些旧样本没有展示新增恢复优势，不能声称实测失锁率下降。回放编译明确使用UTF-8；首次默认GBK编译失败后已重新成功编译再运行。

本候选没有安装，当前用户继续使用0.2.9。没有代发测试或校准鼠标动作。新版本需在用户同步换版窗口后验证：短暂轮廓变化后能否无额外射线移动地恢复；长失锁/歧义是否维持暂停；末次停止的原因与剩余距离是否一致。原0.2.8/0.2.9回退均保留。

## 后续已授权安装及反馈

用户明确同意安装后，于2026-09-12 11:54:46覆盖安装保存的0.2.10，安装前SHA256为22da3d69c98f14f5240bde63c79c78242fcf38bc27176f12c1424277173e8e77，设备versionCode17，PID9906。AirPlay服务、mDNS、Surface附着正常，配对bond12和CCCD恢复true；启动记录无新AndroidRuntime崩溃。没有混入之后正在编写的校准源码，也没有代发输入。

用户随后反馈灰色图标上“暂未看到指针”后不继续靠近，并确认手动点击仍能用。日志存在candidates=0且transportReady=true的观察，同时有用户相对位移报告及成功确认；说明视觉检测未产生候选，不能据无图像文本区分灰色背景混淆与指针吸附形变。11:59:14.068记录一个未可信候选距离68.7采样像素、shape=.9535，状态等待移动相关性；这个候选未被认定为真实指针，不能将其距离当作真实残差。0.2.10没有解决所有图标背景上的丢目标问题，后续以统一校准减少视觉依赖，不继续扩大失锁窗口盲追。
