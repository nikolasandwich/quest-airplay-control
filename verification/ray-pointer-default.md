# 0.2.3-preview：默认相对射线与偏好保存

- 用户已实测确认 0.2.2-preview 开启相对射线后，移动射线可以移动 iPad 鼠标。确认键点击仍未单独验收。
- 本版 versionCode 10。没有保存偏好时，相对射线模式默认开启；手动关闭或开启会保存至应用私有偏好，下次打开按该选择恢复。
- 默认模式开启不等于启用鼠标控制。连接、armed、前台焦点、视频帧、传输门禁及显式关闭按钮均保留。
- APK 构建及 lint 通过。本次仅更改偏好读取/保存和版本标识，未增加重复实现逻辑的测试。
- 本版尚未安装；等待同步覆盖安装的短暂中断窗口。未操作正在运行的应用或发送输入。
